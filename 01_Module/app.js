// const add = require("./math")

// We need to mention the same name as per the export file.
// const { add, mul, div, subs } = require("./math")

// Another Method                               
const math = require("./math")

console.log(math.add(5, 10));

// common js