const add = (num1, num2)=>{
    return num1+num2
}

const subs = (num1, num2)=>{
    return num1-num2
}

const div = (num1, num2)=>{
    return num1/num2
}

const mul = (num1, num2)=>{
    return num1*num2
}

// module.exports.add = add
// module.exports.subs =subs

module.exports={
    add, mul, div, subs
}